"""Repository layer for City Pulse.

Each repository module encapsulates database access for a specific aggregate
or feature area (users, events, interactions, trends, regions, etc.).
Repositories should not contain business rules; they only read/write data.
"""

