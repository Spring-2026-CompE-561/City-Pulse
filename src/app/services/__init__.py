"""Service layer for City Pulse.

Each service module contains business logic that orchestrates repositories and
infrastructure code. Routers should depend on services instead of talking to
the database directly.
"""

